In doodle god, you combine two elements to make new elements. you first choose an element, and then select a second one. If they are compatible, a new element will form. Else, the game will give an error message.
